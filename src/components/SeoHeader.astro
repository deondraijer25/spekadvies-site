---
/**
 * SEO Header Component
 * Handles the generation of meta tags, Open Graph, and Twitter Cards to ensure optimal SEO
 * and rich link previews across all platforms. Canonical URLs are automatically resolved.
 */
interface Props {
  title: string;
  description: string;
  image?: string; // Optional custom fallback image
  type?: "website" | "article"; // Default "website"
}

const { 
  title, 
  description, 
  image = "/assets/spekadvies-share-image.jpg", // Default social share image
  type = "website" 
} = Astro.props;

// Determine absolute canonical URL using Astro.site (configured in astro.config.mjs)
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const imageURL = new URL(image, Astro.site);
---

<!-- Global SEO Meta Tags -->
<title>{title}</title>
<meta name="title" content={title} />
<meta name="description" content={description} />
<link rel="canonical" href={canonicalURL} />

<!-- Open Graph / Facebook / LinkedIn -->
<meta property="og:type" content={type} />
<meta property="og:url" content={canonicalURL} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={imageURL} />
<meta property="og:site_name" content="Spekadvies" />
<meta property="og:locale" content="nl_NL" />

<!-- Twitter / X -->
<meta property="twitter:card" content="summary_large_image" />
<meta property="twitter:url" content={canonicalURL} />
<meta property="twitter:title" content={title} />
<meta property="twitter:description" content={description} />
<meta property="twitter:image" content={imageURL} />

<!-- Standard Sitemap Integration -->
<link rel="sitemap" href="/sitemap-index.xml" />
